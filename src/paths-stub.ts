// TEMPORARY STUB — replaced in Task 11 by cli/version.ts (for VERSION) and
// getDashboardDir() (for the dashboard path resolution that previously used
// PROJECT_ROOT). This file exists only between Task 6 and Task 11 to keep
// typecheck passing in CI.
export const VERSION = "0.1.0";
export const PROJECT_ROOT = process.cwd();
