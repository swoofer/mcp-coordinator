name: Tests

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    strategy:
      matrix:
        node-version: [20, 22]
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4

      - uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: "pnpm"

      - run: pnpm install --frozen-lockfile

      - run: pnpm test:ci

      - run: pnpm build

  # sdk/ has its own npm-managed package (own lockfile, vitest runner) —
  # it's not part of the pnpm workspace test suite above, so it needs its
  # own install+test steps. engines >=20, so run it on the same floor as
  # the main matrix's low end.
  sdk-test:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"
          cache-dependency-path: sdk/package-lock.json

      - run: npm ci
        working-directory: sdk

      - run: npm test
        working-directory: sdk

  # Smoke test: a fresh contributor without native build tools (no Rust
  # toolchain, no python-distutils) must still be able to `pnpm build`.
  # Catches regressions where new code accidentally hard-depends on an
  # optionalDependency like `tree-sitter`.
  build-no-native:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: "pnpm"

      - run: pnpm install --frozen-lockfile --ignore-scripts

      - run: pnpm build
